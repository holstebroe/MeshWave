<UserControl x:Class="MeshWave.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto"
             Background="{StaticResource BackgroundColor}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Import Local Music Folder"
                        Click="ImportMyMusic_Click"
                        Visibility="{Binding CanImportMyMusic, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{StaticResource PrimaryButton}"
                        FontSize="16"
                        Padding="20,12"
                        Width="220"
                        Margin="0,0,10,0"/>
                <Button Content="Import Single File"
                        Click="ImportMyMusicFile_Click"
                        Visibility="{Binding CanImportMyMusic, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{StaticResource PrimaryButton}"
                        FontSize="16"
                        Padding="20,12"
                        Width="220"
                        Margin="0,0,10,0"/>
            </StackPanel>

            <TextBlock Text="{Binding ImportSingleFileStatus}"
                       Margin="0,8,0,0"
                       Foreground="{StaticResource TextSecondaryColor}"
                       Visibility="{Binding CanImportMyMusic, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <TextBlock Text="{Binding SyncStatus}"
                       Margin="0,4,0,0"
                       Foreground="#27AE60"
                       Visibility="{Binding CanImportMyMusic, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <TextBlock Text="Search tracks/albums"
                       Margin="0,12,0,4"
                       Foreground="{StaticResource TextSecondaryColor}"
                       FontStyle="Italic"/>
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,12"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="Select artist -> select album -> tracks"
                   FontStyle="Italic"
                   Foreground="{StaticResource TextSecondaryColor}"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="🎤 Artists" Style="{StaticResource SectionHeader}"/>
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Artists}"
                         SelectedItem="{Binding SelectedArtist, Mode=TwoWay}"
                         DisplayMemberPath="Name"
                         VerticalContentAlignment="Stretch"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            </Grid>

            <Grid Grid.Column="1" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="💿 Albums" Style="{StaticResource SectionHeader}"/>
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Albums}"
                         SelectedItem="{Binding SelectedAlbum, Mode=TwoWay}"
                         MouseDoubleClick="OnAlbumDoubleClick"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                                    <Button Content="▶"
                                            Width="22" Height="22"
                                            FontSize="10"
                                            Click="AlbumPlayButton_Click"
                                            Padding="0"
                                            VerticalAlignment="Center"
                                            Margin="0,0,6,0"
                                            Style="{StaticResource DiscreteIconButton}"/>
                                    <Border Width="36" Height="36" CornerRadius="4" ClipToBounds="True" Background="{StaticResource SurfaceColor}">
                                        <Image Source="{Binding CoverPath}" Stretch="UniformToFill"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding TrackCount, StringFormat={}{0} tracks}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding ReleaseBadge}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Version, StringFormat=v{0}}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="🎵 Tracks" Style="{StaticResource SectionHeader}"/>
                <ListView Grid.Row="1"
                          ItemsSource="{Binding Tracks}"
                          MouseDoubleClick="OnTrackDoubleClick"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Edit Metadata"
                                      Click="EditMetadata_Click"
                                      Visibility="{Binding PlacementTarget.DataContext.CanImportMyMusic, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <MenuItem Header="Remove from Library"
                                      Command="{Binding PlacementTarget.DataContext.RemoveTrackFromLibraryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Visibility="{Binding PlacementTarget.DataContext.CanImportMyMusic, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}"/>
                            <MenuItem Header="Re-download"
                                      Command="{Binding PlacementTarget.DataContext.ReDownloadTrackCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Visibility="{Binding PlacementTarget.DataContext.CanImportMyMusic, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource InverseBoolToVis}}">
                                <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PlacementTarget.SelectedItem.IsRemovedFromLibrary, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                                <Setter Property="IsEnabled" Value="True"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </MenuItem.Style>
                            </MenuItem>
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="#" Width="36">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="▶"
                                                Width="20"
                                                Height="20"
                                                FontSize="10"
                                                Padding="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                IsEnabled="{Binding CanPlay}"
                                                Click="TrackPlayButton_Click"
                                                Style="{StaticResource DiscreteIconButton}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Title" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="36" Height="36" CornerRadius="3"
                                                    ClipToBounds="True"
                                                    Background="{StaticResource SurfaceLightColor}"
                                                    Margin="0,0,10,0">
                                                <Grid>
                                                    <Image Source="{Binding CoverPath}" Stretch="UniformToFill"/>
                                                    <TextBlock Text="♪"
                                                               FontSize="16"
                                                               Foreground="{StaticResource TextSecondaryColor}"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding CoverPath}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}"
                                                           FontWeight="SemiBold" FontSize="13"
                                                           TextTrimming="CharacterEllipsis" MaxWidth="130"/>
                                                <TextBlock Text="{Binding StatusBadge}"
                                                           FontSize="11"
                                                           Foreground="{StaticResource AccentColor}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Artist" Width="130">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Artist}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryColor}"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Album" Width="130">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding AlbumName}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryColor}"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Version" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Version, StringFormat=v{0}}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextSecondaryColor}"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Duration" Width="70">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Duration, StringFormat=mm\\:ss}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryColor}"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Plays" Width="46">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PlayCount}"
                                                   FontSize="12"
                                                   TextAlignment="Right"
                                                   HorizontalAlignment="Right"
                                                   Foreground="{StaticResource TextSecondaryColor}"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
