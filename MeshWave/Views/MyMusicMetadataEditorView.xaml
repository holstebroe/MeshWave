<UserControl x:Class="MeshWave.Views.MyMusicMetadataEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundColor}">
    <Border Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="16">
        <StackPanel>
            <TextBlock Text="✏️ My Music Metadata Editor" Style="{StaticResource SectionHeader}" FontSize="20" Margin="0,0,0,12"/>

            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Artist"/>
            <TextBox Text="{Binding Artist, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Album"/>
            <TextBox Text="{Binding Album, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Genre"/>
            <TextBox Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Year"/>
            <TextBox Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Description"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="70" TextWrapping="Wrap"/>

            <Button Content="Save Metadata" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
