<UserControl x:Class="MeshWave.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundColor}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource SurfaceColor}" CornerRadius="16" Padding="24" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="🎵 MeshWave" FontSize="38" FontWeight="Bold"/>
                <TextBlock Text="Decentralized music sharing with a modern local-first workflow"
                           FontSize="16"
                           Foreground="{StaticResource TextSecondaryColor}"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="🔥 My Music" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="Import, organize, and manage your own tracks." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="🌐 Community Library" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="Browse shared artists and albums from the network." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="▶ Playback" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="Waveforms, comments and timeline interaction." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock Text="Status" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="18"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
