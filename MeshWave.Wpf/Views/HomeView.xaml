<UserControl x:Class="MeshWave.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundColor}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource SurfaceColor}" CornerRadius="16" Padding="0" Margin="0,0,0,16" ClipToBounds="True">
            <Grid>
                <!-- Splash logo as background, scales to fill width -->
                <Image Source="/Assets/LargeSplashLogo.png"
                       Stretch="UniformToFill"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       MaxHeight="200"
                       Opacity="0.55"/>
                <!-- Text overlay on top -->
                <StackPanel Margin="24" VerticalAlignment="Bottom">
                    <TextBlock Text="MeshWave" FontSize="38" FontWeight="Bold"
                               Foreground="White">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="Decentralized music sharing for musicians"
                               FontSize="16"
                               Foreground="#DDDDDD"
                               Margin="0,8,0,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.9"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" BorderThickness="0" Click="MyMusicPanel_Click" Cursor="Hand" Margin="0,0,8,0" Background="Transparent">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Border Padding="16">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InnerViewPanel}">

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                    <Setter Property="Background" Value="#FF2D2D2D"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="🔥 Local Music" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Import, organize, and manage your own tracks." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Button>

            <Button Grid.Column="1" BorderThickness="0" Click="LibraryPanel_Click" Cursor="Hand" Margin="8,0,8,0" Background="Transparent">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Border Padding="16">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InnerViewPanel}">

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                    <Setter Property="Background" Value="#FF2D2D2D"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="🌐 Community Library" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Browse shared artists and albums from the network." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Button>

            <Button Grid.Column="2" BorderThickness="0" Click="PlaybackPanel_Click" Cursor="Hand" Margin="8,0,0,0" Background="Transparent">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Border Padding="16">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource InnerViewPanel}">

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                    <Setter Property="Background" Value="#FF2D2D2D"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Text="▶ Playback" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Waveforms, comments and timeline interaction." Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Button>
        </Grid>

        <Border Style="{StaticResource InnerViewPanel}" Grid.Row="2" Padding="20">
            <StackPanel>
                <TextBlock Text="Status" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="18"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
