<Window x:Class="MeshWave.Wpf.Views.NetworkDiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mesh Diagnostics"
        Height="720" Width="1120"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundColor}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Mesh Diagnostics"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryColor}"
                   Margin="0,0,0,8"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Padding="12,6"
                    Margin="0,0,10,0"
                    VerticalAlignment="Top"/>
            <TextBox Grid.Column="1"
                     Text="{Binding SummaryText, Mode=OneWay}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="80"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="{StaticResource SurfaceColor}"
                     BorderBrush="{StaticResource SurfaceLightColor}"
                     Foreground="{StaticResource TextSecondaryColor}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="430"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Peers"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryColor}"
                               Margin="0,0,0,8"/>

                    <ListView Grid.Row="1"
                              ItemsSource="{Binding Peers}"
                              SelectedItem="{Binding SelectedPeer, Mode=TwoWay}"
                              BorderThickness="0"
                              Background="Transparent"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource SurfaceLightColor}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryColor}"/>
                                            <TextBlock Text="  " />
                                            <TextBlock Text="{Binding PeerTypeLabel}" Foreground="{StaticResource TextSecondaryColor}"/>
                                            <TextBlock Text=" · " Foreground="{StaticResource TextSecondaryColor}"/>
                                            <TextBlock Text="{Binding OnlineLabel}" Foreground="{StaticResource TextSecondaryColor}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding UserId}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Endpoint}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                        <TextBlock Text="{Binding ManifestLabel}" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                        <TextBlock Text="{Binding PublishedSummary}" FontSize="12" Foreground="{StaticResource AccentColor}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Recent messages / exchanges"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryColor}"
                               Margin="0,0,0,8"/>

                    <TextBox Grid.Row="1"
                             Text="{Binding SelectedPeerLogText, Mode=OneWay}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="{StaticResource SurfaceLightColor}"
                             BorderBrush="{StaticResource SurfaceLightColor}"
                             Foreground="{StaticResource TextPrimaryColor}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>

